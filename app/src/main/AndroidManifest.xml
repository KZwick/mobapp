<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.robboba.mha">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestMainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MoodEval"
            android:parentActivityName=".TestMainMenu">
        </activity>
        <activity android:name=".ProInfo"
            android:parentActivityName=".TestMainMenu">
        </activity>
        <activity android:name=".MedInfo"
            android:parentActivityName=".TestMainMenu">
        </activity>
        <activity android:name=".PillTrack"
            android:parentActivityName=".TestMainMenu">
        </activity>
        <activity android:name=".UserReport"
            android:parentActivityName=".TestMainMenu">
        </activity>
        <activity android:name=".LogIn"
            android:parentActivityName=".TestMainMenu">
        </activity>
        <activity android:name=".MainMenu"></activity>

        <receiver android:name=".AlertReceiver"/>
    </application>

</manifest>